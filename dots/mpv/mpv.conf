# Hardware decoding
hwdec=auto-safe

# GPU output
vo=gpu-next

# Caching for slow network drives (phone videos over SMB, etc.)
cache=yes
cache-sec=30

# Performance
video-sync=display-resample
interpolation=yes